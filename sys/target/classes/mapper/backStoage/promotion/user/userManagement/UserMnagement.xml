<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gxa.modules.sys.mapper.backStage.user.UserManagementMapper">
    <resultMap id="searchResultMap" type="com.gxa.modules.sys.entity.backStage.user.UserManagement">
        <id column="id" property="id"/>
        <result column="real_name" property="userName"/>
        <result column="header_location" property="headImg"/>
        <result property="phoneNumber" column="phone_number"/>
        <result property="status" column="status"/>
        <result property="createTime" column="status"/>
        <result property="version" column="version"/>
    </resultMap>
    <!--账户信息-->
    <select id="search" resultMap="searchResultMap">
        SELECT u.id,real_name,header_location,phone_number,`status`,`create_time`,h.`version` FROM
         `t_user` AS u,`t_user_headerImage` AS h
         WHERE u.id = h.t_user_id
        <if test="createTime != null and createTime != ''">
            and  <![CDATA[create_time < #{createTime}]]>
        </if>
        <if test="username != null and username != ''">
            and real_name like concat('%',#{username},'%')
        </if>
        LIMIT #{page},#{limit}
    </select>
    <select id="queryById" resultMap="searchResultMap">
        SELECT u.id,real_name,header_location,phone_number,`status`,`create_time`,h.`version` FROM `t_user` AS u,`t_user_headerImage` AS h WHERE u.id = h.t_user_id
        and u.id = #{id} and h.`version` = #{version}
    </select>
    <!--账号状态的修改-->
</mapper>