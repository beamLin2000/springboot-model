<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gxa.modules.sys.mapper.backStage.promotion.timeLimitedSecondKill.TimeLimitedSecondKillMapper">

    <update id="updateVersion">
        update `promotion_limited_time_flash_deal` set `version` = `version`+1 where `id` = #{id}
    </update>
</mapper>